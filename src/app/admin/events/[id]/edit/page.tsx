import { notFound } from "next/navigation";import { AdminPageHeader } from "@/components/admin/admin-page-header";import { EventForm } from "@/components/admin/events/event-form";import { EVENT_SELECT } from "@/lib/supabase/events";import { createClient } from "@/lib/supabase/server";import type { EventItem } from "@/types/events";export default async function EditEventPage({params}:Readonly<{params:Promise<{id:string}>}>){const {id}=await params,supabase=await createClient(),{data,error}=await supabase.from("events").select(EVENT_SELECT).eq("id",id).maybeSingle();if(error||!data)notFound();const item=data as EventItem,{id:itemId,created_at,updated_at,...values}=item;void created_at;void updated_at;return <><AdminPageHeader description={`Actualizá “${item.title}”.`} title="Editar evento"/><EventForm id={itemId} initialValues={values}/></>}
