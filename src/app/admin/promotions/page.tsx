import Link from "next/link";import {AdminPageHeader}from "@/components/admin/admin-page-header";import{PromotionDeleteButton}from "@/components/admin/promotions/promotion-delete-button";import{togglePromotionActive,togglePromotionFeatured}from "./actions";import{createClient}from "@/lib/supabase/server";import{promotionStatus}from "@/lib/supabase/promotions";import{argentinaAdminDateTimeFormatter}from "@/lib/utils/news-dates";import type{Promotion}from "@/types/promotions";
const states={inactive:"Inactiva",scheduled:"Programada",current:"Vigente",expired:"Vencida"};const places:Record<string,string>={top_bar:"Barra superior",home:"Home",plans:"Planes",conectar_play:"Conectar Play"};const date=(v:string|null)=>v?argentinaAdminDateTimeFormatter.format(new Date(v)):"Sin fecha";
export default async function PromotionsPage({searchParams}:Readonly<{searchParams:Promise<{success?:string;error?:string}>}>){const params=await searchParams,supabase=await createClient();const{data,error}=await supabase.from("promotions").select("id,title,slug,summary,description,image_path,button_text,button_url,starts_at,ends_at,active,featured,placements,display_order,created_at,updated_at").order("featured",{ascending:false}).order("display_order",{ascending:true}).order("starts_at",{ascending:false,nullsFirst:false});const items=(data??[])as Promotion[];return <><div className="flex flex-col items-start gap-4 sm:flex-row sm:justify-between"><AdminPageHeader description="Programá beneficios y elegí dónde se muestran." title="Promociones"/><Link className="rounded-xl bg-orange-600 px-5 py-3 font-bold text-white" href="/admin/promotions/new">Nueva promoción</Link></div>{params.success&&<p className="mb-5 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800" role="status">La acción se completó correctamente.</p>}{params.error&&<p className="mb-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800" role="alert">{params.error==='image-cleanup'?'El registro se guardó, pero no se pudo limpiar la imagen anterior.':'No se pudo completar la acción.'}</p>}{error?<p className="rounded-2xl border bg-white p-8">No pudimos cargar las promociones.</p>:!items.length?<div className="rounded-2xl border border-dashed bg-white p-10 text-center"><h2 className="text-xl font-bold">Todavía no hay promociones.</h2></div>:<div className="grid gap-4">{items.map(item=><article className="rounded-2xl border bg-white p-5 shadow-sm" key={item.id}><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-bold">{item.title}</h2><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold">{states[promotionStatus(item)]}</span>{item.featured&&<span className="rounded-full bg-orange-100 px-2.5 py-1 text-xs font-bold text-orange-800">Destacada</span>}</div><dl className="mt-3 grid gap-x-8 gap-y-2 text-sm sm:grid-cols-4"><div><dt className="font-semibold">Inicio</dt><dd>{date(item.starts_at)}</dd></div><div><dt className="font-semibold">Finalización</dt><dd>{date(item.ends_at)}</dd></div><div><dt className="font-semibold">Ubicaciones</dt><dd>{item.placements.map(p=>places[p]).join(', ')||'Solo listado'}</dd></div><div><dt className="font-semibold">Orden</dt><dd>{item.display_order}</dd></div></dl></div><div className="flex flex-wrap gap-4 text-sm"><Link className="font-bold text-orange-700" href={`/admin/promotions/${item.id}/edit`}>Editar</Link><form action={togglePromotionActive}><input name="id" type="hidden" value={item.id}/><input name="value" type="hidden" value={String(!item.active)}/><button className="font-bold">{item.active?'Desactivar':'Activar'}</button></form><form action={togglePromotionFeatured}><input name="id" type="hidden" value={item.id}/><input name="value" type="hidden" value={String(!item.featured)}/><button className="font-bold">{item.featured?'Quitar destacado':'Destacar'}</button></form><PromotionDeleteButton id={item.id} title={item.title}/></div></div></article>)}</div>}</>}
