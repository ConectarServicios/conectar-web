"use client";
import Link from "next/link";import {useActionState,useState} from "react";
import {savePromotion} from "@/app/admin/promotions/actions";import {normalizePromotionSlug} from "@/lib/validations/promotions";import {formatArgentinaDateTimeLocal} from "@/lib/utils/news-dates";import type{PromotionActionState,PromotionFormValues}from "@/types/promotions";
const input="mt-2 w-full rounded-xl border border-slate-300 bg-white px-3.5 py-2.5 text-slate-950 shadow-sm outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-200";
const locations=[['top_bar','Barra promocional superior'],['home','Home'],['plans','Planes'],['conectar_play','Conectar Play']] as const;
export function PromotionForm({id,initialValues}:Readonly<{id?:string;initialValues?:PromotionFormValues}>){const[state,action,pending]=useActionState(savePromotion,{} as PromotionActionState);const[slug,setSlug]=useState(initialValues?.slug??"");const[manual,setManual]=useState(Boolean(id));const error=(key:string)=>state.fieldErrors?.[key];return <form action={action} className="space-y-6">{id&&<><input name="id" type="hidden" value={id}/><input name="current_image_path" type="hidden" value={initialValues?.image_path??""}/><input name="current_slug" type="hidden" value={initialValues?.slug}/></>}{state.message&&<p className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800" role="alert">{state.message}</p>}
<fieldset className="rounded-2xl border bg-white p-6 shadow-sm"><legend className="px-2 text-lg font-bold">Contenido</legend><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold sm:col-span-2">Título *<input className={input} defaultValue={initialValues?.title} name="title" onChange={e=>{if(!manual)setSlug(normalizePromotionSlug(e.target.value))}} required/>{error('title')&&<span className="text-xs text-red-700">{error('title')}</span>}</label><label className="text-sm font-semibold sm:col-span-2">Slug *<input className={input} name="slug" onChange={e=>{setManual(true);setSlug(normalizePromotionSlug(e.target.value))}} value={slug} required/><span className="mt-1 block text-xs font-normal text-slate-500">/promociones/{slug||'slug'}</span>{error('slug')&&<span className="text-xs text-red-700">{error('slug')}</span>}</label><label className="text-sm font-semibold sm:col-span-2">Bajada / resumen *<textarea className={`${input} min-h-24`} defaultValue={initialValues?.summary} name="summary" required/>{error('summary')&&<span className="text-xs text-red-700">{error('summary')}</span>}</label><label className="text-sm font-semibold sm:col-span-2">Descripción completa *<textarea className={`${input} min-h-64`} defaultValue={initialValues?.description} name="description" required/>{error('description')&&<span className="text-xs text-red-700">{error('description')}</span>}</label><label className="text-sm font-semibold sm:col-span-2">Imagen JPG, PNG o WebP (máximo 5 MB)<input accept="image/jpeg,image/png,image/webp" className={input} name="image" type="file"/>{error('image')&&<span className="text-xs text-red-700">{error('image')}</span>}</label><label className="text-sm font-semibold">Texto del botón<input className={input} defaultValue={initialValues?.button_text??""} name="button_text"/></label><label className="text-sm font-semibold">URL del botón<input className={input} defaultValue={initialValues?.button_url??""} name="button_url" placeholder="/contacto o https://..."/>{error('button_url')&&<span className="text-xs text-red-700">{error('button_url')}</span>}</label></div></fieldset>
<fieldset className="rounded-2xl border bg-white p-6 shadow-sm"><legend className="px-2 text-lg font-bold">Vigencia</legend><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Inicio<input className={input} defaultValue={formatArgentinaDateTimeLocal(initialValues?.starts_at??null)} name="starts_at" type="datetime-local"/><small className="mt-1 block font-normal text-slate-500">Horario de Argentina (Buenos Aires).</small>{error('starts_at')}</label><label className="text-sm font-semibold">Finalización<input className={input} defaultValue={formatArgentinaDateTimeLocal(initialValues?.ends_at??null)} name="ends_at" type="datetime-local"/><small className="mt-1 block font-normal text-slate-500">Horario de Argentina (Buenos Aires).</small>{error('ends_at')}</label><label className="flex items-center gap-3 text-sm font-semibold"><input className="size-5 accent-orange-600" defaultChecked={initialValues?.active??true} name="active" type="checkbox"/> Activa</label></div></fieldset>
<fieldset className="rounded-2xl border bg-white p-6 shadow-sm"><legend className="px-2 text-lg font-bold">Visibilidad</legend><div className="grid gap-5 sm:grid-cols-2"><label className="flex items-center gap-3 text-sm font-semibold sm:col-span-2"><input className="size-5 accent-orange-600" defaultChecked={initialValues?.featured} name="featured" type="checkbox"/> Destacada</label>{locations.map(([value,label])=><label className="flex items-center gap-3 text-sm font-semibold" key={value}><input className="size-5 accent-orange-600" defaultChecked={initialValues?.placements.includes(value)} name="placements" type="checkbox" value={value}/>{label}</label>)}<label className="text-sm font-semibold sm:col-span-2">Orden de visualización<input className={input} defaultValue={initialValues?.display_order??0} min="0" name="display_order" type="number"/>{error('display_order')}</label></div></fieldset><div className="flex justify-end gap-3"><Link className="rounded-xl border px-5 py-2.5 font-bold" href="/admin/promotions">Cancelar</Link><button className="rounded-xl bg-orange-600 px-5 py-2.5 font-bold text-white disabled:opacity-60" disabled={pending}>{pending?'Guardando…':'Guardar promoción'}</button></div></form>}
