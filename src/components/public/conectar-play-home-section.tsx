import Link from "next/link";
import { ConectarPlayPlans } from "@/components/public/conectar-play-plans";
import type { ConectarPlayPlan, ConectarPlaySettings } from "@/types/conectar-play";

export function ConectarPlayHomeSection({ settings, plans, unavailable }: { settings: ConectarPlaySettings | null; plans: ConectarPlayPlan[]; unavailable: boolean }) { if (!settings && plans.length === 0 && !unavailable) return null; return <section className="scroll-mt-24 bg-slate-50 py-20 sm:py-24" id="conectar-play" aria-labelledby="home-play-title"><div className="public-container"><p className="public-eyebrow">Conectar Play</p><div className="mt-3 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><h2 className="public-heading" id="home-play-title">{settings?.channel_count ? `${settings.channel_count} canales para disfrutar donde quieras.` : "Entretenimiento donde quieras."}</h2>{settings?.short_description && <p className="mt-4 max-w-2xl text-lg leading-8 text-slate-600">{settings.short_description}</p>}{settings?.simultaneous_devices && <p className="mt-2 text-slate-600">Disponible en hasta {settings.simultaneous_devices} dispositivos simultáneos.</p>}</div><Link className="public-button-primary shrink-0" href="/conectar-play">Conocer Conectar Play</Link></div>{plans.length > 0 && <div className="mt-10"><ConectarPlayPlans plans={plans} /></div>}{unavailable && plans.length === 0 && <p className="public-empty-state">La información no está disponible temporalmente.</p>}</div></section>; }
