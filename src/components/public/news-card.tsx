import Image from "next/image";
import Link from "next/link";
import type { NewsItem } from "@/types/news";
export function NewsCard({ item, imageUrl }: Readonly<{ item: NewsItem; imageUrl: string | null }>) { return <article className="group overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg">{imageUrl && <div className="relative aspect-[16/9] overflow-hidden bg-slate-100"><Image alt={`Portada de ${item.title}`} className="object-cover transition duration-300 group-hover:scale-105" fill sizes="(max-width: 768px) 100vw, 33vw" src={imageUrl}/></div>}<div className="p-6"><p className="text-xs font-black tracking-widest text-orange-700 uppercase">{item.category}</p><h2 className="mt-2 text-xl font-black text-slate-950">{item.title}</h2>{item.excerpt && <p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{item.excerpt}</p>}<p className="mt-4 text-sm font-semibold text-slate-500">{item.published_at && new Intl.DateTimeFormat("es-AR",{dateStyle:"long"}).format(new Date(item.published_at))}</p><Link className="mt-5 inline-flex font-bold text-orange-700 hover:text-orange-900" href={`/noticias/${item.slug}`}>Leer más <span aria-hidden="true">→</span></Link></div></article>; }
