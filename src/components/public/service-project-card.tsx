import Image from "next/image";
import Link from "next/link";
import { ServiceAreaIcon } from "@/components/public/service-area-icon";
import type { ServiceProject } from "@/types/service-projects";
export function ServiceProjectCard({ project, imageUrl, areaIcon }: Readonly<{ project: ServiceProject; imageUrl: string | null; areaIcon: string | null }>) {
  const external = project.public_url?.startsWith("http") ?? false;
  return <article className="group flex h-full flex-col overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="relative aspect-[16/10] overflow-hidden bg-gradient-to-br from-[#0b2440] to-blue-700">{imageUrl ? <Image alt={project.title} className="object-cover transition duration-300 group-hover:scale-105" fill sizes="(max-width: 768px) 100vw, (max-width: 1280px) 50vw, 33vw" src={imageUrl}/> : <div className="flex h-full items-center justify-center text-white"><div className="rounded-3xl bg-white/10 p-5 ring-1 ring-white/20"><ServiceAreaIcon icon={areaIcon}/></div></div>}{project.featured && <span className="absolute top-4 right-4 rounded-full bg-orange-500 px-3 py-1 text-xs font-black text-white shadow">Destacado</span>}</div><div className="flex flex-1 flex-col p-6 sm:p-7">{project.project_type && <p className="text-xs font-black tracking-widest text-orange-700 uppercase">{project.project_type}</p>}<h3 className="mt-2 text-2xl font-black text-slate-950">{project.title}</h3>{project.short_description && <p className="mt-3 flex-1 leading-7 text-slate-600">{project.short_description}</p>}{project.public_url && <Link className="mt-6 inline-flex w-fit items-center font-bold text-orange-700 hover:text-orange-900" href={project.public_url} {...(external ? { target: "_blank", rel: "noopener noreferrer" } : {})}>Conocer más <span className="ml-2" aria-hidden="true">→</span></Link>}</div></article>;
}
