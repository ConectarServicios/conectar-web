export type ConectarPlaySettings = { id: string; active: boolean; channel_count: number; simultaneous_devices: number; web_url: string | null; short_description: string | null; compatibility_text: string | null; incompatible_tv_text: string | null; onn_enabled: boolean; onn_sale_price: number | null; onn_rental_price: number | null; onn_description: string | null; support_text: string | null };
export type ConectarPlayPlan = { id: string; name: string; slug: string; description: string | null; promotional_price: number; promotion_label: string | null; promotion_discount_percent: number | null; promotion_duration_months: number | null; promotion_start: string | null; promotion_end: string | null; includes_football: boolean; active: boolean; featured: boolean; display_order: number };
export type ConectarPlayPack = { id: string; name: string; description: string | null; price: number | null; active: boolean; display_order: number };
export type ConectarPlayFaq = { id: string; question: string; answer: string; active: boolean; display_order: number };
export type ConectarPlayActionState = { message?: string; success?: boolean; fieldErrors?: Record<string, string> };
